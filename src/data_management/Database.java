package data_management;

public class Database {
    public static void main(String[] args) {
        System.out.println("Create 4 CRUD functions each for three tables. Create database with database name 'Insightify'. The constructor should create the connection with database. There should be two constructor, the first one should be default constructor which creates and stores the connection object in instance variable. The second one should take the password of database as parameter, the create the connection object and store it in instance variable.(This is so that if we are testing the program on two different systems, and database password is different, we don't have to everytime change the code). There should be no main function in this class.");
    }
}
